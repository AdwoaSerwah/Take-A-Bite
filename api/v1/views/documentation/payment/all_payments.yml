---
get:
  summary: "Retrieve all payments"
  description: "Returns a list of all payment objects"
  responses:
    200:
      description: "A list of payment objects"
      schema:
        type: "array"
        items:
          $ref: "#/definitions/Payment"
